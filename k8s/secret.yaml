apiVersion: v1
kind: Secret
metadata:
  name: quoteiq-secrets
  namespace: quoteiq
type: Opaque
# Values must be base64-encoded: echo -n "value" | base64
# DO NOT commit real values — populate via:
#   kubectl create secret generic quoteiq-secrets --namespace quoteiq \
#     --from-literal=DATABASE_URL="..." \
#     --from-literal=JWT_SECRET="..." \
#     --from-literal=GROQ_API_KEY="..." \
#     --from-literal=OPENAI_API_KEY="..." \
#     --from-literal=RABBITMQ_URL="..." \
#     --dry-run=client -o yaml | kubectl apply -f -
stringData:
  DATABASE_URL: ""
  JWT_SECRET: ""
  GROQ_API_KEY: ""
  OPENAI_API_KEY: ""
  RABBITMQ_URL: ""
