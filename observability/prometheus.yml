global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: quoteiq-backend
    # host.docker.internal resolves to the Windows host from inside the Docker container
    static_configs:
      - targets: ['host.docker.internal:4000']
    metrics_path: /metrics
