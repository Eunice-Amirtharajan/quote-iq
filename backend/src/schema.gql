# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Client {
  city: String
  company: String!
  country: String
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
  phone: String
  updatedAt: DateTime!
}

input ClientInput {
  city: String
  company: String!
  country: String
  email: String!
  name: String!
  phone: String
}

input CreateQuotationInput {
  clientId: String!
  items: [QuotationItemInput!]!
  notes: String
  taxRate: Float
  title: String!
  validUntil: DateTime
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  createClient(input: ClientInput!): Client!
  createQuotation(input: CreateQuotationInput!): Quotation!
  deleteClient(id: ID!): Boolean!
  deleteQuotation(id: ID!): Boolean!
  login(email: String!, password: String!): User!
  logout: Boolean!
  updateClient(id: ID!, input: ClientInput!): Client!
  updateQuotationStatus(id: ID!, input: UpdateQuotationStatusInput!): Quotation!
}

type Query {
  client(id: ID!): Client
  clients: [Client!]!
  me: User!
  quotation(id: ID!): Quotation
  quotations: [Quotation!]!
}

type Quotation {
  client: Client
  createdAt: DateTime!
  createdBy: User
  id: ID!
  items: [QuotationItem!]
  notes: String
  quotationNumber: String!
  status: QuotationStatus!
  subtotal: Float!
  taxAmount: Float!
  taxRate: Float!
  title: String!
  total: Float!
  updatedAt: DateTime!
  validUntil: DateTime
}

type QuotationItem {
  description: String!
  id: ID!
  lineTotal: Float!
  quantity: Float!
  sortOrder: Int!
  unitPrice: Float!
}

input QuotationItemInput {
  description: String!
  quantity: Float!
  unitPrice: Float!
}

enum QuotationStatus {
  APPROVED
  DRAFT
  EXPIRED
  REJECTED
  SENT
}

enum Role {
  ADMIN
  SALES_MANAGER
  SALES_REP
}

input UpdateQuotationStatusInput {
  note: String
  status: String!
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
  role: Role!
  updatedAt: DateTime!
}